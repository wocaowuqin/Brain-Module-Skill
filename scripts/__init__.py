"""Compatibility script package.

New commands should live in ``scripts/train``, ``evaluate``, ``data``,
``checks`` or ``runtime``. Existing root scripts remain import-compatible
until the staged migration is complete.
"""
