# Legacy compatibility area

This directory is read-only compatibility code. New implementation work must
go to `core/`, `envs/`, `trainer/`, `sdn/`, or the top-level `bms/` package.
The legacy tree is retained until two consecutive releases show no external
imports.
